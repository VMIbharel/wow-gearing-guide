<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 200 200">
  <!-- Shield -->
  <path d="M100 20 L150 45 L150 110 C150 145 100 175 100 175 C100 175 50 145 50 110 L50 45 Z"
    fill="none" stroke="currentColor" stroke-width="4" stroke-linejoin="round"/>

  <!-- Sword -->
  <g>
    <!-- Blade -->
    <rect x="95" y="50" width="10" height="95" fill="currentColor"/>
    <!-- Crossguard -->
    <rect x="80" y="142" width="40" height="6" fill="currentColor"/>
    <!-- Pommel -->
    <circle cx="100" cy="162" r="5" fill="currentColor"/>
  </g>

  <style>
    svg { color: currentColor; }
    @media (prefers-color-scheme: dark) {
      svg { color: #FFF; }
    }
    @media (prefers-color-scheme: light) {
      svg { color: #000; }
    }
  </style>
</svg>
